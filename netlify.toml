[build]
  command = "npm ci && npm --prefix frontend run build"
  publish = "frontend/dist"

# Optional: cache node_modules to speed up builds
[build.environment]
  NODE_VERSION = "20"

# SPA fallback is handled by frontend/public/_redirects
